<%= form_with(model: codigo, local: true) do |form| %>
  <% if codigo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(codigo.errors.count, "error") %> prohibited this codigo from being saved:</h2>

      <ul>
      <% codigo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
